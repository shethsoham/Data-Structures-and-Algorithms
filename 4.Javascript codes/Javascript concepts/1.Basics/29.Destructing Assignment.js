// Template literals  - ${}
// Backticks `
// anything inside {} bracket is javascript

const person ={
    name: "Soham Sheth",
    age: 23
};

const greeting = `Hello,my name is ${person.name}!
I am ${person.age} years old.`;

console.log(greeting);